import HomeRoute from 'routes/Home'

describe('(Route) Home', () => {
  let _component;

  beforeEach(() => {
    _component = HomeRoute.component()
  });

  // it('', () => {
  //   expect(typeof HomeRoute).to.equal('')
  // });
  //
  // it('t', () => {
  //   expect(_component.type).to.equal('')
  // })
});
